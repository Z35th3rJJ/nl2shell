---
status: accepted
---

# 统一命令审查

所有计划展示、执行前复检、验证和评测必须消费同一个 `CommandReview`，由它结合受限 Bash 分析、文件系统事实、确定性安全规则和只能提高风险的模型建议形成最终结论。解析细节保持为 module 内部实现，安全规则不可由运行时配置削弱；旧的分析与决策接口仅作为临时兼容 adapter，避免规则继续散落或调用方依赖解析器结构。

## Consequences

审查结果不缓存；确认后的复检若提高授权等级，必须重新确认，无交互入口不得执行。
