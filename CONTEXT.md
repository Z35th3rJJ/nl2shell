# nl2shell

nl2shell 将自然语言任务转换为可审查的 Bash 命令，并在执行前形成一致的安全结论。

## Language

**命令分析（Command Analysis）**:
从命令中提取的客观事实，例如程序、参数、路径角色、覆盖目标和影响；不包含是否允许执行的结论。

**命令审查（Command Review）**:
在命令执行前，结合命令分析、工作区和风险信号形成的完整安全结论；不包含用户询问、候选选择或界面展示。
_Avoid_: 用“命令分析”指代完整安全结论
